# Each Amplify app has its own build spec in its appRoot directory:
#   packages/platform/amplify.yml   — auth / platform backend
#   packages/todo-backend/amplify.yml — data backend
#   apps/todo-app/amplify.yml       — React frontend
